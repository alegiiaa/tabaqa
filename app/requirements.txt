# ── Tabaqa backend — Python dependencies ──────────────────────────────────
# The API + the demo pipeline/scoring run on just the first block (the pipeline
# and scorecard are intentionally pure-stdlib). The rest are the production /
# stretch integrations described in the README.

# API (required to serve /v1/score, /v1/profile)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Cohort store (optional) — serves the 500k sandbox population from PostgreSQL
# (api/cohortdb.py, `python -m api.cohortdb loadpg`). Imported lazily; without it
# the sandbox falls back to the SQLite file, then the on-demand generator.
psycopg[binary]>=3.2

# AI augmentation (optional) — Claude long-tail enricher + the financial-intelligence
# narrative (/v1/insights). The core pipeline/eval import this lazily and run fine
# WITHOUT it; install it + set ANTHROPIC_API_KEY to light up the real-Claude path.
anthropic>=0.49

# Scoring / data validation — fit + validate the PD model on the real Berka
# default dataset (eval/berka_train.py → eval/DATA_REPORT.md: CV AUC 0.858 / KS 0.562).
# pymysql reads the public CTU "relational" MariaDB (no login). optbinning is the
# production WOE-binned scorecard; pin it to a scikit-learn it supports.
optbinning>=0.19
scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
pymysql>=1.1

# Stretch: XGBoost + SHAP explainability
# xgboost>=2.0
# shap>=0.45

# Arabic cleaning (production long-tail; the demo uses deterministic rules)
# camel-tools>=1.5
# arabic-reshaper>=3.0
# python-bidi>=0.4
